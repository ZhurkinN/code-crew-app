micronaut:
  application:
    name: codeCrew
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
      swagger-ui:
        paths: classpath:META-INF/swagger/views/swagger-ui
        mapping: /swagger-ui/**
  security:
    authentication: cookie
    enabled: false
datasources:
  default:
    url: jdbc:postgresql://localhost:5432/code-crew-app
    username: postgres
    password: Iissupe5
    driver-class-name: org.postgresql.Driver
    db-type: postgres
    dialect: POSTGRES
  warehouse:
    url: jdbc:h2:mem:test
    username: root
    password: root
    driverClassName: org.h2.Driver
    dialect: h2

netty:
  default:
    allocator:
      max-order: 3
liquibase:
  enabled : true
  datasources:
    default:
      change-log: classpath:db/changelog/changelog.yaml
    warehouse:
      change-log: classpath:db/changelog/changelog-h2.yaml
